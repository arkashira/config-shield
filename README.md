# Config Shield

Config Shield is a real-time failure-mode detection system for LLM responses. It detects hallucination and toxicity in responses and sends alerts via webhook and UI notification.

## Usage

1. Install the package using `pip install .`
2. Run the main script using `python -m config_shield`
